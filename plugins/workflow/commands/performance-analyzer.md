---
name: performance-analyzer
description: Invoke the performance-analyzer sub-agent for database queries, N+1 issues, caching, and optimization review.
---

Invoke the `bootstrap-workflow:performance-analyzer` sub-agent via the Task tool. Pass the
user's request verbatim as the task `prompt`. Use `subagent_type: "bootstrap-workflow:performance-analyzer"`.

User's request: $ARGUMENTS
