---
name: code-review-specialist
description: Invoke the code-review-specialist sub-agent for quality, security, and maintainability review.
---

Invoke the `bootstrap-workflow:code-review-specialist` sub-agent via the Task tool. Pass the
user's request verbatim as the task `prompt`. Use `subagent_type: "bootstrap-workflow:code-review-specialist"`.

User's request: $ARGUMENTS
