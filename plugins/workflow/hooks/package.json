{
    "name": "bootstrap-workflow-hooks",
    "version": "1.0.0",
    "description": "Claude workflow safety guards and build-artifact gates for the bootstrap workflow plugin",
    "private": true,
    "type": "module",
    "scripts": {
        "check": "bun run --bun tsc --noEmit",
        "test": "bun test"
    },
    "dependencies": {
        "minimatch": "^10.1.1",
        "unbash": "2.2.0"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}
