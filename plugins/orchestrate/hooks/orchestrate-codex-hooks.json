{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"Before spawn_agent with a missing model or effort: pipe the task into node '${PLUGIN_ROOT}/skills/orchestrate/scripts/pick-dispatch.mjs' --runtime codex. On decision route, use its pick only for missing fields. Decision inherit means no picker override: preserve explicit choices subject to existing caps, otherwise use normal native judgment and omit fields for the runtime to resolve; select an effort shim only when you deliberately choose that effort. Preserve roles and retained workers.\"",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "^(spawn_agent|collaborationspawn_agent)$",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${PLUGIN_ROOT}/hooks/require-codex-context.mjs\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
