Use `/team-plan` for this task:

Rename one internal helper and update its four call sites. The helper is not exported, no
serialized format or external API changes, and the existing focused unit test covers all callers.

Explain the proportional execution path you would record in `plan.md`. State whether the cohesive
work requires parallel builders and identify the review that must occur before approval.
