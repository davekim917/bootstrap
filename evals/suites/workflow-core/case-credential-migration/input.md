Use `/team-plan` for this task:

Change one credential-record field and migrate its existing stored values. A wrong migration can
lock users out, and rollback must not lose the old values.

Explain the proportional execution path you would record in `plan.md`, including the boundaries
that deserve hardening, rollback evidence, and the review that must occur before approval.
